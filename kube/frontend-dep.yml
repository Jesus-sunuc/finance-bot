apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  namespace: finance-bot
spec:
  replicas: 1
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
    spec:
      containers:
      - name: frontend
        image: jesus6190/finance-frontend:latest
        ports:
        - containerPort: 80
